# Design import — provenance

Imported 2026-07-17 from claude.ai/design project 4010831f-44d8-4ca5-9c74-1fd1a51f1df4
("Multi-platform app design project", owner Thomas) via DesignSync.

- `myCase - Design Decisions.md` — CANON handoff doc (tokens, rules, screen inventory, deltas vs UX spec).
- `myCase Mobile.dc.html` — iPhone canvas, newest turn at top. **TRUNCATED at the 256 KiB API cap**:
  all sections 8a–1a are present; the cut lands inside the late 1-series (glass exploration/1d
  onboarding markup — marked stale "pending retrofit" in the decisions doc anyway). Full copy
  lives only in the design project.
- `myCase Tablet.dc.html` — iPad canvas, complete.
- `ios-frame.jsx`, `support.js` — canvas runtime/frame components (context for reading the canvases;
  not app code).
- NOT imported (already elsewhere or derivable): uploads/* (UX spec = docs/superpowers/specs/…;
  style guide + interviewer console bundles live in the mycase/ project dir), extracted/*.template.html
  (readable copies — fetch from the design project if needed).

These are REFERENCE artifacts for front-end implementation. Do not edit; design iteration happens
in the claude.ai/design project.
