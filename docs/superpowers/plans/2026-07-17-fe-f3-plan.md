# F3 — Case tab · Execution Plan

Branch `fe/f3-case` · worktree `/Users/thomaskgould/dev/fe-f3` · cut at 9cff198.
Canon: Decisions doc §1/§2-3b/§5/§7-1b (CANON), frontend-execution §5-F3, contract
sheet (bgap-b1/b2/b3/b4). Baselines locked: backend **655**, iOS **500/0**, xcodegen
idempotent. Reports override briefs.

Design law (reviewer rubric = Decisions §5): staircase mark only (no icons/emoji),
square content corners / capsule glass, secondary = underline (never outlined box),
≤1 glass hero per screen, ≤3 greens per screen, tabular numerals on timers/codes,
NO population counts. Tokens only — no hex / `Color(red:)` outside F0 token files.
Copy VERBATIM (quick-pick labels, expiry copy, "never gated" note, "Interviewer
decides", serif rule line). Persona data (Amara §3) = Previews/fixtures ONLY; live
screens bind to API.

Model split per §5-F3: SPINE = sonnet (transcription of 3b/1b), SHEETS = opus
(JUDGMENT). Every screen task: VM/view tests green + foreground bounded build +
simctl screenshot + reviewer canvas-anchor compare.

Parallel boundary: F5 owns SessionView/SessionViewModel + recap screens. F3 consumes
`.recap(sessionID)` route only; provides a MARK-bounded interim recap stub destination
(F5 replaces at merge). Do NOT edit session/recap files. Shared-file edits
(RootShell, APIClient, Models, CaseRoomApp docblock) stay in bounded `// MARK: F3`
blocks; conflicts settle at merge.

---

## T1 — Data & networking layer (sonnet)

Files: `Models.swift`, `SessionModels.swift`, `APIClient.swift` (bounded MARK blocks),
new `State/CaseTabViewModel.swift`, new fixtures `State/CaseFixtures.swift`, tests.

1. Extend `Proposal` (Models.swift): make `caseId: Int?`, `caseTitle: String?`
   (contract: nullable). Add `direction: String` (received|sent), `state: String`
   (pending|accepted|declined|countered|expired), `claimToken: String?`,
   `counterTimes: [Date]?`, `counterBy: Int?`, `counteredAt: Date?` — CodingKeys
   snake_case. Update the one call site (`SessionsViewModel.accept` uses
   `proposal.caseTitle`/`fromName`) — nil-coalesce title to a placeholder; F3 replaces
   SessionsView so keep it compiling only.
2. `PairToken` (SessionModels.swift): add `shortCode: String` (CodingKey `short_code`).
   Extend `pairCreate` decode accordingly.
3. New `RecapItem` (Codable): sessionId, caseId, caseTitle, interviewerName,
   grade: Double?, finalizedAt: Date, viewedAt: Date? (GET /api/v1/recaps →
   `{recaps:[...]}` oldest-first, unread-only). Add `APIClient.recaps() -> [RecapItem]`.
4. Gate helper: typed `enum CaseGateError: Error { case blockedByRecap(Int) }`. Add a
   decode path in APIClient's error handling that parses 409 body
   `{"detail":{"blocked_by_recap": <sessionID>}}` → throws `.blockedByRecap(id)`.
   Applies to acceptProposal, claimProposal, pairClaim, (swap/accept — F5's, skip).
5. New APIClient methods: `counterProposal(id:times:[Date])`,
   `claimProposal(token:) -> ClaimResult{proposalId,state,sessionId:Int?,accepted,
   needsNegotiation,fromUserId}`. `acceptProposal` already exists (keep; it must now
   surface the gate error). `recommendations(exclude:)`, `availability()`,
   `setFree`/`clearFree`, `pairStatus` already exist.
6. `CaseTabViewModel` (@Observable, injectable `CaseTabService` protocol; APIClient
   conforms; default + fixture init):
   - loads: `recaps()` (gate card = first unread), `proposals()` (bucket by
     direction/state → `pendingReceived` [received+pending], `sentAwaiting`
     [sent+pending/countered], surface counterTimes), `sessions(scope:"upcoming")`
     → nextUp (soonest) + upcoming[], `sessions(scope:"past")` → history[].
   - actions: `accept(_:at:)` (existing calendar side-effect via `CalendarAdding`,
     catch `.blockedByRecap` → set `gatedRecapSessionID`), `decline(_:)`,
     `counter(_:times:)` ("New time"), all catching the gate error.
   - `gatedRecapSessionID: Int?` published for the view to navigate `.recap(id)`.
7. `CaseFixtures.swift`: persona §3 data (recap-gate: Ski resort, T. Becker, 4.1/5;
   NEXT UP tonight 19:00 vs M. Lindqvist "Low-cost carrier…"; PENDING T. Becker Thu
   18:00 dental + S. Park tonight 21:30; sent-awaiting; UPCOMING; HISTORY) for
   Previews + screenshot hatch.
8. Tests (`CaseTabViewModelTests`): bucketing, gate-409 → gatedRecapSessionID (mock
   throws `.blockedByRecap`), accept removes+refreshes+calls calendar, counter,
   decline, recap-gate card selection. Mock service, no network/EventKit.

Evidence: iOS suite green (500 + new); build. No screenshot (data layer).

## T2 — Phone Case spine, canvas 3b `*3` (sonnet)

Files: new `Views/CaseTabView.swift` (+ subviews), `RootShell.swift` (bounded MARK:
replace `case .caseTab: SessionsView()` with `NavigationStack(path:$router.casePath){
CaseTabView() … .navigationDestination(for:AppRoute.self){ if case .recap(let id)=…}}`),
interim `Views/RecapGateStub.swift` (MARK: F3 interim — F5 replaces).

- Slim glass verb bar (the screen's ONE glass hero + ONE filled button): filled ink
  capsule **"Get cased now"** + underline-text **"Case someone"** / **"Schedule"**.
- recap-gate card (the only card; sanctioned 2nd filled exception): serif lede from
  the recap ("Structure held. The quant went soft…"), grade, interviewer; tap →
  `router.casePath.append(.recap(sessionID))`.
- flat **NEXT UP** block (hairline, not glass) w/ **Swap** underline action.
- **UPCOMING** rows: accepted rows rise-in (Motion.rise stagger); each row an
  add-to-calendar affordance (underline, triggers VM calendar path).
- **PENDING** rows (received): text **Accept** / **New time** / **Decline**; New time
  opens a small time-picker → `counter`. Sent-awaiting rows: dashed-era hairline +
  "awaiting reply".
- **HISTORY** rows (past sessions, greyed, tabular date).
- Header scroll-fade + hidden scrollbars + 120px bottom pad (Decisions §1 chrome).
- Verb bar buttons present three sheets via `@State` enum; T2 ships MARK-bounded
  placeholder sheet shells (title + close) — T3/T4/T5 replace bodies.
- gate: on `vm.gatedRecapSessionID` change → append `.recap(id)`, clear.
- DEBUG hatch `-CaseFixtures` (fake-auth + fixture-backed VM; no dev server), mirror
  F8's `-GroupPageFixtures` pattern in RootShell/CaseRoomApp.
- Tests: view-model-driven bucket rendering counts; gate nav sets casePath.

Evidence: suite green; foreground build; iPhone-17 screenshot `.superpowers/sdd/f3/
shots/t2-phone-spine.png`; report affirms vs canvas 3b `*3`.

## T3 — "Get cased now" sheet (opus JUDGMENT)

`Views/GetCasedNowSheet.swift` + `State/GetCasedNowViewModel.swift`.
GlassSheet: QR square (CoreImage `CIQRCodeGenerator`, no dep — encodes claim URL),
6-char **short_code** (tabular, grouped `K7Q-4TN` style), inline LIVE-NOW board
(availability `others` → rows w/ **Ping** → toast **"expires in 2 h"**), expiry copy
(from `expiresAt`), **copy-link** (UIPasteboard). Uses `pairCreate(caseId:)`
(case-less by default; prefill case optional) + `availability()`. One glass hero rule:
the sheet IS the glass; inner QR/board flat. Screenshot `t3-getcased.png`.

## T4 — "Case someone" sheet (opus JUDGMENT) + F4 prefill seam

`Views/CaseSomeoneSheet.swift` + VM. GlassSheet: **Scan QR** primary (camera →
`pairClaim(short_code/token)`; simulator has no camera → manual short-code entry
fallback + `-CaseFixtures` static shot), open invites (sent interviewer proposals /
claim links), interviewer log (past sessions where role=interviewer), verbatim
**"never gated" note**. Prefill seam: add `router.caseSomeonePrefillCaseID: Int?`
(MARK: F3); edit `CaseDetailView` CTA ("Case someone with this" / "Get re-cased
anyway") to set it + `selection=.caseTab`; `CaseTabView.onAppear`/onChange auto-opens
this sheet with the case preselected. Screenshot `t4-casesomeone.png`.

## T5 — "Schedule composer" sheet (opus JUDGMENT)

`Views/ScheduleComposerSheet.swift` + VM. GlassSheet: **WHO** chips (connections /
free-now people), **WHEN** quick-picks **Now · 1h · Tonight 8pm · Pick a time**
(verbatim), **CASE:** segmented **"Interviewer decides"** ⟂ **Request:[rec]**
(recommendations, decode key `title`), serif rule line **"The app is the calendar,
not the conversation."** Submits `POST /api/proposals` (to_user_id/case_id/from_role/
proposed_times). Screenshot `t5-schedule.png`.

## T6 — Tablet 1b `recT tProp pingT toastT` (sonnet)

`CaseTabView` size-class `.regular` branch (reuse T2–T5 components). Glass tray hero:
300px verb column (ink "Get cased now" + two glass verbs) | vertical divider | inline
LIVE-NOW board (**"PINGS EXPIRE IN 2 H"**, Ping→toast). Two-column spine grid `1fr
1fr gap 36`: left = recap card → NEXT UP (Swap) → UPCOMING (rise-in); right = PENDING
(Accept/New time/Decline) → HISTORY. Case tab-circle green ring = F0 active state
(already). July-17 tablet fixtures (DAY 13; Nordic 7.2 done; T. Becker today 18:00).
iPad Pro 11" screenshot (landscape; portrait fallback per F4 TCC note)
`t6-tablet.png`.

## Close-out
Full iOS suite + backend suite green; kill dev server (8103); shut down iPad sim;
whole-branch adversarial Opus review (Decisions §5 rubric + gate/contract fidelity) →
fix loop; report `docs/superpowers/sdd/fe-f3-report.md`; ledger
`.superpowers/sdd/f3/progress.md`; shots under `.superpowers/sdd/f3/shots/`; commit
per task (small imperative). NEVER push/merge. xcodegen + commit both after any
project.yml change (new files under existing groups need no project.yml change if
globbed — verify; if project.yml lists files explicitly, add + regen).

Done-when (phase): all 6 tasks green w/ shots; both suites at/above baseline;
adversarial review APPROVED-FOR-MERGE (0 Critical/Important open); report + ledger
committed; servers/sims cleaned up.

---

## Plan-review dispositions (Opus, verdict SOUND-WITH-FIXES — applied)

- **C1 (Proposal blast radius).** T1.1: DON'T claim "one call site." Add an explicit
  `Proposal.init(...)` defaulting the 6 new fields to `nil` (mirror `DashboardStats`
  init pattern), keep `caseId`/`caseTitle` optional. Nil-coalesce ALL read sites:
  `SessionsViewModel.swift:60`, `Intents/CaseRoomEntities.swift:147`,
  `Views/SessionsView.swift:105`. Fix construction sites in tests:
  `SessionsViewModelTests.swift:63`, `IntentsTests.swift:138` & `:220`. Also make
  `AcceptedSession.sessionUrl`/`icsUrl` OPTIONAL (M13: case-less/negotiating accept
  omits them). Build must stay green → 500/0 preserved before T2 replaces the tab.
- **I2 (go(.recap) steering).** DEFERRED to F5 by my hard boundary (brief: do NOT
  touch App Intents / deep-link steering — `CaseRoomIntents.go(.recap)` is that).
  F3 wires ONLY its own entry points to append `casePath` (VM `gatedRecapSessionID`
  + recap-gate card tap). Push/deep-link `go(.recap)` steering is F5's (it "wires the
  recap presentation"). Documented deviation.
- **I3 (HISTORY scope).** Backend `api_v1.py:361` supports `scope ∈ {upcoming,
  recent}` — NOT "past". T1.6 HISTORY uses `sessions(scope:"recent")`.
- **I4 (QR payload — PINNED contract T3↔T4).** QR encodes `caseroom://pair?code=
  <short_code>`; the 6-char `short_code` is ALSO shown as text. T4 scanner accepts
  either a `caseroom://pair?code=` URL OR a bare 6-char code → `pairClaim` with
  `short_code`. (`pairClaim` body accepts token OR short_code.)
- **I5 (calendar for sessions).** T1.6 adds `addToCalendar(_ session: SessionSummary)`
  VM action (guard `scheduledAt`; reuse `CalendarAdding`).
- **M6.** NO explicit `CodingKeys` on Proposal/PairToken — the decoder uses global
  `.convertFromSnakeCase`; a partial enum would break un-listed keys. Just add fields.
- **M7.** Reviewer enforces inner-flat on T4/T5 (sheet is the only glass; glass chips
  are the sanctioned §1 chip recipe, not heroes). Recap-gate card = flat filled card,
  the sanctioned 2nd filled exception — NOT a second glass hero.
- **M8 (Ping — PINNED).** Ping = `POST /api/proposals` to that user (`to_user_id`,
  `case_id:null`, `from_role:"candidate"`, `proposed_times:[now]`). Toast "expires in
  2 h" is cosmetic (availability window). Verbatim copy "PINGS EXPIRE IN 2 H"
  (tablet)/"expires in 2 h" (phone) from canvas.
- **M9.** T6: include the §7-1b "accept crosses columns" PENDING→UPCOMING animation.
- **M10.** Countered-SENT proposals: Case tab shows "awaiting reply" only (no
  accept-the-counter action) — matches canon 3b; record as deliberate deviation.
- **M11.** 3b canvas VERIFIED present (not truncated): "never gated — recaps only
  block your candidate seat." + "The app is the calendar, not the conversation."
  confirmed verbatim. Implementers grep the canvas for exact strings.
- **M12.** Close-out full suite: the F4-escalated Xcode-27 `SessionViewModelTests`
  mic-TCC hang may recur. F3 touches `SessionsViewModel` (not `SessionViewModel`); if
  the suite hangs at that class, it is the known pre-existing block — note, don't call
  red. Wedge-recovery per brief.
