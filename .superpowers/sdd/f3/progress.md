# F3 — Case tab · SDD Progress Ledger

Branch: fe/f3-case · Worktree: /Users/thomaskgould/dev/fe-f3 · Cut at 9cff198
DB: caserepo_fe_f3 · Dev port: 8103 · Sims: iPhone 17 942222D4 / iPad Pro 11" 653F37B8
Interpreter: /Users/thomaskgould/dev/Case-Repo-App-Experience/.venv/bin/python

## Baseline (bootstrap complete)
- DB caserepo_fe_f3 created; db/schema.sql + all db/migrations/0*.sql applied ON_ERROR_STOP clean; seed_caseroom_dev.py → users a/b/c@yale.edu + case 1.
- Backend suite: **655 passed** (`pytest tests/ -q`, 17.2s) — MATCHES required baseline 655. Finish line = 655 (F3 is FE-only; adds no backend tests) OR 655+N if any.
- iOS suite: **500 tests, 0 failures** (xcodebuild test, iPhone 17 942222D4) — MATCHES required baseline 500/0. Finish line = 500 + new VM tests.
- xcodegen generate idempotent (no diff after regen).
- Mic granted to study.mycase / .CaseRoomTests / com.apple.dt.xctest.tool on 942222D4.

## Reuse inventory (existing, do NOT rebuild)
- SessionsViewModel (proposals inbox + upcoming, accept/decline, EventKit via CalendarAdding) — Case tab currently renders SessionsView(); F3 replaces the tab body.
- APIClient: proposals(), acceptProposal(id:scheduledAt:), declineProposal(id:), pairCreate/pairStatus/pairClaim, availability()/setFree/clearFree, recommendations(exclude:).
- PairViewModel, FreeNowViewModel; CalendarWriter (EventKitCalendarWriter / CalendarAdding).
- AppRoute already has .caseTab, .recap(Int), .sessionTakeover(Int) (F1-pinned). caseTab currently NOT wrapped in a NavigationStack in RootShell (selectedTab: case .caseTab: SessionsView()).

## Seams inherited
- F4: case-detail CTA "Case someone with this" + "Get re-cased anyway" route to .caseTab plainly (no id). F3 OWNS case-prefill → add a router field; update CaseDetailView CTA to set it; Case tab auto-opens Case-someone prefilled. (No new AppRoute — enum pinned.)
- F5 parallel (~/dev/fe-f5): owns SessionView/SessionViewModel + recap screens. F3 navigates .recap(sessionID) on any blocked_by_recap 409; provides a MARK-bounded interim recap destination stub (F5 replaces at merge). Do NOT edit session/recap files.

## Plan
- docs/superpowers/plans/2026-07-17-fe-f3-plan.md (T1 data → T2 phone spine → T3/T4/T5 sheets → T6 tablet → close-out). Contract sheet extracted (scout). Opus plan-review: SOUND-WITH-FIXES (1 Crit + 4 Imp + 8 Min) — all dispositioned in the plan's "Plan-review dispositions" section. Key fixes: Proposal explicit init + optional case fields (blast radius = 5 sites not 1); HISTORY scope="recent" (NOT "past"); QR payload pinned caseroom://pair?code=<short_code>; go(.recap) steering DEFERRED to F5 per hard boundary; Ping = now-proposal.

## Tasks
- **T1 DONE** (commits 04de029, c81b115). Data layer: Proposal (optional case fields + direction/state/claimToken/counterTimes/counterBy/counteredAt + explicit defaulted init), AcceptedSession urls optional, PairToken.shortCode, RecapItem, ClaimResult, CaseGateError.blockedByRecap(Int) wired into accept/claim/pairClaim only (nested {"detail":{"blocked_by_recap":id}} @ 409). CaseTabViewModel + CaseTabService + CaseFixtures. APIClient.recaps()/claimProposal/counterProposal. Suite **509/0** (500+9). Opus review APPROVE-WITH-NITS (0 Crit/Imp; 4 minors: VM not @MainActor = mirrors SessionsViewModel precedent; 2 dead gate-catch arms in decline/counter = spec-directed; minor test gaps; claimProposal token path-interp low-risk). xcodeproj stays gitignored/regenerable (repo convention — only Package.resolved tracked).
- **T2 DONE** (phone spine, canvas 3b `*3`). New `Views/CaseTabView.swift` (verb bar = the one glass hero + one filled button "Get cased now" + underline "Case someone"/"Schedule"; recap-gate card = flat filled square-corner card, sanctioned 2nd filled exception, NOT glass — plan-review M7; NEXT UP FOR YOU + Swap stub-toast; UPCOMING rows rise-in staggered + add-to-calendar; PENDING Accept/New time/Decline + dashed sent "awaiting reply" rows; HISTORY greyed tabular rows; empty-state serif line) + `CaseSheet` enum (getCased/caseSomeone/schedule, verbatim titles) + placeholder `CaseSheetPlaceholder` shells (`// MARK: T3/T4/T5 fills this`) + `NewTimeSheet` (New-time DatePicker→counter). New `Views/RecapGateStub.swift` (`// MARK: F3 INTERIM`, F5 replaces at merge). RootShell: bounded `// MARK: F3` block replaces `case .caseTab: SessionsView()` with `NavigationStack(path:$router.casePath){ caseTabRoot… }` + `.recap` navigationDestination + `caseTabRoot` (mirrors `groupPageDestination`'s `-CaseFixtures` branch) + added `-CaseFixtures` to the two DEBUG skip-lists (bootstrap race, push-auth alert). CaseRoomApp: `-CaseFixtures` hatch (fake auth + selects `.caseTab`, mirrors `-CommunityFixtures`) + docblock updated. Deliberate deviations (documented in code comments): RecapItem has no serif-quote field, so the gate card's lede binds `caseTitle` not a fabricated quote; Proposal has no recipient-name field, so sent-awaiting rows lead with case title, not "You → X"; NEXT UP binds `vm.nextUp` (no separate recommendation entity in the VM) with the canvas's NEXT UP+Swap chrome; row meta (time/date) is a plain formatter, not canvas's literal "Tonight 19:00 · lobby opens 18:50 · T-6H" relative-time microcopy (not flagged verbatim in the brief); RootShell's `detailOpen` (top-pills/tab-bar hide gate) was NOT extended to `casePath` — left exactly as instructed, so the tab bar/top pills stay visible over the pushed RecapGateStub (F5 should reconsider when it replaces the stub). Suite **522/0** (509+13, `CaseTabViewTests.swift`: `CaseTabSectionVisibility`, `CaseTabCopy.pendingOfferLabel`, `CaseTabGateSteering`, `CaseSheet`). Foreground build green (iPhone 17 942222D4). Screenshot `.superpowers/sdd/f3/shots/t2-phone-spine.png` (`-CaseFixtures -startTab caseTab`) — verb bar, recap-gate card, NEXT UP, UPCOMING, PENDING all present and visually match canvas 3b.
- **T2 review fixes applied** (CHANGES-REQUIRED → addressed, same `CaseTabView.swift`). IMPORTANT-1: restored canvas 3b's leading status dot (green `BlinkDot` on nextUp only, hollow ring on UPCOMING rows) + right-aligned tabular countdown (`CaseTabCopy.countdown(to:now:)`, "T-6H"/"T-3D", 5 new unit tests) on both the NEXT UP row and UPCOMING rows; kept "Add to calendar" (mandated capability, not invented chrome). IMPORTANT-2: recap-gate card is now the screen's ONE glass hero (`.glassPanel(cornerRadius: 26)`, overriding the earlier flat/M7 guess — canvas 3b line 1544 is glass; verb bar re-scoped as glass CHROME, not a hero — filled-button budget still exactly 2). MINOR-3: sent-awaiting row divider is the standard 1px hairline (removed the invented `DashedLine` shape). MINOR-4: `.tabularNumbers()` added to pendingRow/sentAwaitingRow meta + the recap grade line. MINOR-7: Swap toast copy lowercased to "invite sent". Suite **527/0** (522+5 countdown tests). Rebuilt + re-screenshotted `.superpowers/sdd/f3/shots/t2-phone-spine.png` (`-CaseFixtures -startTab caseTab`) — glass recap card, dot+countdown on NEXT UP/UPCOMING, solid hairline sent row all confirmed visually.
- **T2 re-review: APPROVE-WITH-NITS** (fresh Opus, 245fbd0) — both Important resolved, no regressions, 0 Crit/Imp. Nit N1 (countdown 23-24h → "T-24H" not "T-1D"; never hit by fixtures) deferred as cosmetic. Re-shot verified vs canon 3b. **T2 COMPLETE.**
- Sheet pixel-truth extracted → scratchpad/f3-sheets-3b.html. Headers: "GET CASED NOW" / "CASE SOMEONE" / "SCHEDULE FOR LATER — A PROPOSAL". Get-cased = QR hero + code INLINE in serif ("Their scan pairs you instantly. Code K7Q-4TN works too.") + "REMOTE — FREE RIGHT NOW" board (Ping/Copy link underlines); case-someone = filled "Scan their QR" + "Open invites — 1 waiting"(green NEW) + "Your interviewer log" + "Interviewing is never gated — recaps only block your candidate seat."; schedule = WHO/WHEN(Now·1h·Tonight 8pm·Pick a time)/CASE(Interviewer decides ⟂ Request:[rec]) chips + filled "Send the proposal" + "Accept · counter once · decline. The app is the calendar, not the conversation."
- **T3 DONE** (commit 9649b22). Get-cased-now glass sheet: header "GET CASED NOW", real CIQRCodeGenerator QR encoding caseroom://pair?code=<shortCode>, code inline in serif ("Their scan pairs you instantly. Code K7Q-4TN works too."), "REMOTE — FREE RIGHT NOW" board (S. Park/J. Okafor Ping underlines from availability().others), "Anyone — send a link · Copy link", Cancel. GetCasedNowViewModel(prefillCaseId:service:now:schools:writeClipboard:) + GetCasedService. APIClient: pairCreate(caseId:Int?) overload (non-breaking sibling of PairService's Int version) + createNowInvite(toUserId:). -CaseSheet getCased DEBUG hatch. Presentation: .glassSheet r34 bottom-pinned in T2's .sheet(item:), .presentationBackground(.clear). Suite **538/0** (+11). Opus review APPROVE-WITH-NITS (0 Crit/Imp; 3 cosmetic: QR true-black vs ink comment, VM re-alloc idiom, bottom inset). Deviations: live availability FreeUser has no school → "· school" omitted live (fixture-only continuity); QR true-black for scan contrast.
- **T4** (Case someone sheet + FULL F4→F3 prefill seam) — dispatching. Seam: AppRouter gains caseSomeonePrefillCaseID/caseGetCasedPrefillCaseID (bounded MARK: F3, router state only — NOT go()/intents); CaseDetailContent.swift:297 (done→case-someone, open→get-cased) + :325 (re-cased→get-cased); CaseTabView auto-opens the right sheet on prefill.
- **T4 DONE** (commits 768ffe0 + fix 921f059). Case-someone glass sheet: header "CASE SOMEONE", filled "Scan their QR" (AVFoundation scanner; sim/denied → manual 6-char entry fallback), "Open invites — N waiting"(green NEW; received+candidate+pending filter), "Your interviewer log", verbatim "Interviewing is never gated — recaps only block your candidate seat.", Cancel. F4→F3 PREFILL SEAM: AppRouter +caseSomeonePrefillCaseID/caseSomeonePrefillTitle/caseGetCasedPrefillCaseID (bounded MARK, router state only); CaseDetailContent CTA (done→case-someone+title, open/re-cased→get-cased) then go(.caseTab); CaseTabView consumes via CasePrefillSteering pure helper, captures to @State before clearing router fields (no rebuild race, no loop). claimedSessionID→sessionTakeoverID (interim SessionView); gatedRecapSessionID→.recap (defensive). Suite **560/0** (+22). Review 1st pass CHANGES-REQUIRED (I-1: scanned short code posted under `token` not `short_code` → scan→pair dead vs real backend); fix: new pairClaim(shortCode:) posts {"short_code":...} (sibling of untouched pairClaim(token:) — legacy PairScanView unbroken), scanResult routes both URL+bare code there, tests assert short-code path. Re-review APPROVE-WITH-NITS (I-1 fixed, no regression; nit: 1 stale comment). Deviations documented: M-1 "Using this case: <title>" additive prefill line (canvas predates seam), M-2 manual-entry copy (sim fallback), interviewer-log "avg feedback quality" fixture-only (no API metric).
- **T5** (Schedule composer sheet, opus JUDGMENT) — dispatching. Canvas sheetLater3: WHO chips / WHEN (Now·1h·Tonight 8pm·Pick a time) / CASE (Interviewer decides ⟂ Request:[rec]) / "Send the proposal" / "Accept · counter once · decline. The app is the calendar, not the conversation."
- **T5 DONE** (commit 994dfa1). Schedule composer glass sheet (sheetLater3): header "SCHEDULE FOR LATER — A PROPOSAL", WHO glass chips (connections), WHEN quick-picks Now·1h·Tonight 8pm·Pick a time (Pick a time→inline DatePicker), CASE rows Interviewer decides(DEFAULT)⟂Request:<rec.title>(green FOR YOU), filled "Send the proposal", verbatim "Accept · counter once · decline. The app is the calendar, not the conversation.", Cancel. APIClient.sendScheduledProposal(toUserId:caseId:Int?:fromRole:proposedTimes:) — case_id explicit-null (not encodeIfPresent), from_role candidate, ISO proposed_times; createProposal untouched. WHEN semantics: now→[now], 1h→[now+3600], Tonight 8pm→today 20:00 (tomorrow if ≥20:00), Pick a time→[picked]. canSend gates who+when+case. Suite **575/0** (+15). Opus review APPROVE-WITH-NITS (0 Crit/Imp; 3 Min: CaseSheetPlaceholder now dead code, tonight-8pm exact-boundary untested, live Pick-a-time fixed 560pt detent overflow risk). Deviation: CASE tags DEFAULT/FOR YOU (canvas generic o.tagText) — reasonable, ≤1 green.
- **All three sheets COMPLETE.** Accumulated minors deferred to a close-out cleanup pass: T2-N1 (countdown 23-24h edge, cosmetic — likely skip), T4-nit (stale comment), T5-M1 (delete dead CaseSheetPlaceholder), T5-M3 (make schedule sheet body scrollable / resizable detent — real live-path fix).
- **T6 DONE** (commit 59ac85f). Tablet 1b: CaseTabLayout.isTablet(hSize)==.regular → caseTabTablet (phone spine byte-identical via casePhoneSpine). Glass tray hero (300pt verb column "Get cased now"/"Case someone"/"Schedule later" | divider | LIVE-NOW board "LIVE NOW — FREE TO CASE YOU"/"PINGS EXPIRE IN 2 H"/Ping) reusing the shared activeSheet seam; two-column spine (1fr/1fr gap 36) reusing recap/NEXT UP/UPCOMING/PENDING/HISTORY subviews. CaseTabViewModel +liveNow/ping/availability() (additive, non-fatal on failure). July-17 tablet fixture makeTabletViewModel() (gate cleared, T.Becker re-timed today 18:00; phone July-16 untouched). Implementer self-caught+fixed 2 render bugs (unbounded divider height, missing 64pt top pad). Suite **581/0** (+6). iPad Pro 11" 653F37B8 portrait shot (landscape blocked by macOS TCC = F4 precedent; size-class-driven so identical). Opus review APPROVE-WITH-NITS (0 Crit/Imp; 4 min: divider pinned 104pt vs stretch (short if board >2 users), min-free not tabular (matches canvas), fixture left-column sparse pre-accept (illustrative, permitted), NEXT UP recommendation deviation (inherited)).
- **ALL 6 TASKS DONE.** Suite 581/0.
- **Close-out cleanup pass** — dispatching: T5-M3 (schedule sheet scrollable/resizable detent — live Pick-a-time overflow), T6-M1 (tray divider stretch for 3+ live users), T5-M1 (delete dead CaseSheetPlaceholder), T4-nit (stale comment). Skipping cosmetic-only T2-N1 (countdown 23-24h edge) + T6-M2 (min-free tabular matches canvas).
- close-out remainder (full iOS 581 + backend 655 suites, whole-branch adversarial review, report, kill port 8103, shut iPad) — pending.
